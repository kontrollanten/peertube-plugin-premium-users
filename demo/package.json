{
  "name": "tests",
  "version": "1.0.0",
  "description": "",
  "main": "prepare-plugin.js",
  "scripts": {
    "start": "docker compose up --build --remove-orphans --wait",
    "test": "npm start && npx playwright test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.7.5"
  },
  "dependencies": {
    "winston": "^3.15.0"
  }
}
